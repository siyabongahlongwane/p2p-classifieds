import { Stack, Typography } from "@mui/material"

const Dashboard = () => {
    return (
        <Stack>
            <Typography variant="h6">
                Mels to make decision whether or not we subscribe to a CRM service or we get a resource to assist build one
            </Typography>
        </Stack>
    )
}

export default Dashboard